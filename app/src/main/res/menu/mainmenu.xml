<menu xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/refresh"
        android:title="@string/refresh"
        android:icon="@drawable/ic_action_refresh"
        app:showAsAction="always" />
    <!--<group android:checkableBehavior="all" app:showAsAction="never">
        <item android:id="@+id/checkbox_showSummary" android:title="@string/show_summary" android:onClick="onCheckboxClicked"
            android:defaultValue="true">
        </item>
    </group>-->
    <group android:checkableBehavior="all">
        <item android:id="@+id/checkbox_showSummary"
            android:title="@string/show_summary"
            android:checked="true" />
    </group>
</menu>
